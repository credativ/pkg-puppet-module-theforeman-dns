puppet-module-theforeman-dns package
---------------------------------

This repository contains a Debian package with above puppet module.
It can be build with

    dpkg-buildpackage

as one would usually expect.

The module itself can be managed with r10k or librarian-puppet. E.g.

    r10k puppetfile install

will install all modules as listed in the Puppetfile.
